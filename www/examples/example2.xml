<example>
    <title>2-Simple Program</title>
    <program><![CDATA[ % Some simple test Prolog programs
loves(vincent, mia).
loves(marcellus, mia).
loves(pumpkin, honey_bunny).
loves(honey_bunny, pumpkin).

jealous(X, Y) :-
    loves(X, Z),
    loves(Y, Z).


/** <examples>

    loves(X, mia).
    jealous(X, Y).
    jealous(X, Y), X \== Y.
*/]]></program>
    <query>jealous(X, Y).</query>
</example>
